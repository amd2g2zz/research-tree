## 1. Baseline Contract Tests

- [x] 1.1 Add failing tests for baseline identity, public-manifest validation,
  hidden-oracle separation, conservative classification, and aggregate receipts.
- [x] 1.2 Verify the `0.0.1-a1` tag resolves to the committed full baseline
  revision used by the manifest.

## 2. Baseline Implementation

- [x] 2.1 Add the versioned Alpha1 adversarial public manifest with nine
  representative cross-host failure cases and explicit execution dispositions.
- [x] 2.2 Implement manifest validation and the evaluator-owned oracle registry
  with deterministic receipt classification.

## 3. Verification And Evidence

- [x] 3.1 Run the focused Alpha1 baseline test suite and correct failures.
- [x] 3.2 Run strict OpenSpec validation, package validation, and the complete
  repository test suite.
- [ ] 3.3 Record verification receipts, update this checklist, and prepare the
  focused issue PR evidence.
