## 1. Registry Contract

- [x] 1.1 Add focused failing tests for registry lifecycle metadata, top-level checkout coverage, and stable layout diagnostics.
- [x] 1.2 Complete the path-registry envelope/schema and classify tracked roots plus supported local artifact roots.
- [x] 1.3 Reconcile the group-21 acceptance command and delivery surface with the executable checker.

## 2. Read-Only Boundary Checker

- [x] 2.1 Implement deterministic registry, checkout-root, and ignore-policy validation without filesystem mutation.
- [x] 2.2 Validate source/generated/install/runtime/evaluation boundaries and protected local-material reporting.
- [x] 2.3 Add negative fixtures for malformed registry entries, package boundary drift, missing ignores, and unexpected roots.

## 3. Clean Checkout Guidance

- [x] 3.1 Add exact `.gitignore` rules for supported local installation, runtime, build, raw, cache, and untracked evaluation roots.
- [x] 3.2 Document path authority, package/install boundaries, clean-checkout probes, and collision-aware operator migration steps.
- [x] 3.3 Mark the #70 and umbrella group-21 implementation tasks only after their corresponding checks pass.

## 4. Evidence And Verification

- [ ] 4.1 Run the focused checker, package, and documentation acceptance checks; record a source-bound group-21 receipt.
- [x] 4.2 Run strict issue/umbrella OpenSpec, package parity, documentation/governance checks, full regression, and diff checks.
- [ ] 4.3 Mark final verification complete, create the #70 PR to `dev`, merge it, and clean the worktree only after reachability is confirmed.
