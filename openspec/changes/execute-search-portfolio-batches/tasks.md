# Tasks: execute-search-portfolio-batches

## Implementation

- [x] 1. Add immutable method outcomes and dependency-ready batch values bound
  to the existing strict portfolio and registry boundaries.
- [x] 2. Implement typed failure dispositions, fallback selection, and
  degraded single-provider capability reporting without coordinator writes.
- [x] 3. Implement batch coverage, novelty, contradiction, source-quality, and
  unresolved-risk assessment with typed next decisions.

## Verification

- [x] 4. Add focused red/green tests for independent boundaries, all typed
  failures, alternatives, degraded capability, pivot, and serialization.
- [ ] 5. Run the exact group-75 acceptance command and record a source-bound
  receipt after the source commit.
