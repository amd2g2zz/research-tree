## 1. Manifold resolution

- [x] 1.1 Read all current typed registrations grouped by role and reject
  generic latest-kind lookups.
- [x] 1.2 Resolve P0 closure slots, singleton ambiguity, delivery pair lineage,
  acceptance digests, actors, currentness, and quarantine into field diagnostics.
- [x] 1.3 Bind the immutable completion record to the deterministic manifold
  digest and all resolved parent references.

## 2. Regression coverage

- [x] 2.1 Prove generic append lookalikes remain blocked with no completion
  record.
- [x] 2.2 Prove the real assessor/verifier/evaluator/compiler/acceptance writer
  chain completes once and retries idempotently.
- [x] 2.3 Prove ambiguous, malformed, superseded, and quarantined parents
  reopen field-level diagnostics without mutating history.

## 3. Verification and handoff

- [x] 3.1 Run the focused manifold, registration, coordinator, replay, trace,
  and correction regression suite plus lint/format checks.
- [x] 3.2 Record the source-bound group-45 receipt after the final commit.
- [ ] 3.3 Run delivery governance, open the single Issue #158 PR, and merge it
  to `dev` with PR body `Closes #158` only.
