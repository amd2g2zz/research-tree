## 1. Contract And Red Tests

- [ ] 1.1 Replace the unsupported historical task wording with an explicitly
  synthetic, non-historical public-case contract.
- [ ] 1.2 Add failing controls for activation ordering, one open question,
  correction invalidation, task isolation, recursive continuation, attribution,
  delivery provenance, and unavailable runtime handling.

## 2. Governed Fixture

- [ ] 2.1 Add the versioned public case and schema with opaque evaluator
  identity and no raw session material.
- [ ] 2.2 Add the deterministic harness and CLI without invoking a provider.
- [ ] 2.3 Allow group-24 source-bound raw receipts only inside the existing
  ignored evaluation-run root.

## 3. Evidence And Delivery

- [ ] 3.1 Register the runner and group-24 acceptance command.
- [ ] 3.2 Run the accepted control, record the named GLM runtime blocker, and
  retain a compact public unavailable summary.
- [ ] 3.3 Complete the group-24 task/verification records only from the
  source-bound receipt and run strict governance checks.
