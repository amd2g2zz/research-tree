# canonicalize-findingpack-decision-delivery-consumers

Issue #180: canonicalize decision, delivery, and strict-delivery Finding Pack test consumers onto direct `RunLedger` fixtures and `CanonicalFindingPackCompiler`, while retaining assurance and pre-#181 readiness as isolated legacy-runtime coverage.
