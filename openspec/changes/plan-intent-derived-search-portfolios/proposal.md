## Why

The strict #163 contract can represent a portfolio but cannot derive one from
the current intent and decision evidence. Planning must stay pure until later
children own acquisition, durable lineage, and execution.

## What Changes

- Add a pure, bounded intent-derived SearchPortfolio planner.
- Record revision-bound query rewrites, assumptions, evidence classes, effects,
  closure oracles, and stop/replan triggers without raw query text.
- Reopen a human decision only for material authority, safety, or requester
  outcome changes.
- Register group 74 / issue #185 without changing parent group 27 or #83.

## Non-Goals

- Retrieval, batch assessment, persistence, coordinator dispatch, CLI, and
  durable acquisition lineage.
