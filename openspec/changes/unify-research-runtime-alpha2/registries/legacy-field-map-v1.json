{
  "schema_version": 1,
  "mappings": [
    {
      "legacy_surface": "RunStore.round.json.id",
      "canonical_field": "ResearchRun.run_id",
      "rule": "copy identifier after validation; retain original round_id alias"
    },
    {
      "legacy_surface": "RunStore.round.json.parent_round_id",
      "canonical_field": "ResearchRun.parent_run_id",
      "rule": "map only after parent digest resolves; otherwise quarantine"
    },
    {
      "legacy_surface": "decision_map.target.decision_slots[].id",
      "canonical_field": "DecisionSlot.slot_id",
      "rule": "copy id; preserve old payload under migration metadata"
    },
    {
      "legacy_surface": "decision_map.target.decision_slots[].alternatives",
      "canonical_field": "DecisionSlot.options",
      "rule": "normalize ordered alternatives; retain original order and digest"
    },
    {
      "legacy_surface": "decision_map.target.decision_slots[].validation",
      "canonical_field": "DecisionSlot.required_oracles",
      "rule": "convert only registered oracle references; unresolved values become legacy_unverified"
    },
    {
      "legacy_surface": "research-tree-state.status",
      "canonical_field": "ResearchRun.lifecycle_state",
      "rule": "searching to autonomous_research; delivery_pending preserved; complete to completed only after current gates; blocked requires reason"
    },
    {
      "legacy_surface": "native-or-hermes.state.json.status",
      "canonical_field": "HostEvent plus coordinator lifecycle",
      "rule": "import as host observation; never copy complete as canonical completion"
    },
    {
      "legacy_surface": "FindingPack.validation_result.status",
      "canonical_field": "OracleRun.verdict",
      "rule": "retain observation but require executable oracle binding before closure"
    },
    {
      "legacy_surface": "delivery.kind=human-brief",
      "canonical_field": "delivery.kind=human-research-report",
      "rule": "legacy_unverified import; alpha2 writer rejects legacy kind"
    }
  ]
}
