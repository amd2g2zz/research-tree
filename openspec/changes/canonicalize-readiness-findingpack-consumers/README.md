# canonicalize-readiness-findingpack-consumers

Issue #181: migrate readiness and strict-evidence Finding Pack test consumers
to direct canonical `RunLedger` fixtures after #180, while retaining the
explicit legacy coverage owned by assurance and export.
